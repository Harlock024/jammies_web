---
import Layout from "@/layouts/Layout.astro";
const { id } = Astro.props;
const user_id = Number(id);
---

<Layout title={`User-${user_id}`}>
    <h1>Profile</h1>
    <p>User ID: {user_id}</p>
</Layout>
